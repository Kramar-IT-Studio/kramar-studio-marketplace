---
description: Generate a C4 diagram (context/container/component) — alias for `/archforge:diagram c4-<level>`. Kept for backward compatibility; new work can use `/archforge:diagram` directly with any type.
argument-hint: "<level: context|container|component> <subject>"
---

# /archforge:c4

This command is an alias for `/archforge:diagram c4-<level> <subject>`.

It generates exactly one C4 diagram. For other diagram types — sequence, state, ER, deployment — use `/archforge:diagram <type> <subject>` directly.

## Usage

```
/archforge:c4 context "the storefront and its surrounding services"
/archforge:c4 container "the editor backend and its dependencies"
/archforge:c4 component "the AI gateway internals"
```

These are equivalent to:

```
/archforge:diagram c4-context "the storefront and its surrounding services"
/archforge:diagram c4-container "the editor backend and its dependencies"
/archforge:diagram c4-component "the AI gateway internals"
```

## Steps

1. Parse the first argument as the C4 level (`context`, `container`, or `component`).
2. Forward to `/archforge:diagram` logic with type `c4-<level>`.

If the level argument is missing or invalid, ask the user to pick. Don't render multiple levels.

## Why both commands exist

`/archforge:c4` predates `/archforge:diagram`. It's kept as an alias so old workflows and muscle memory don't break. The substantive logic is in `/archforge:diagram` and the `architectural-diagrams` skill.

For new work — use `/archforge:diagram` directly. It's the single entry point for all five diagram types.
